apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: nosqls.platform.local.lab
spec:
  group: platform.local.lab
  scope: Namespaced
  names:
    kind: NoSql
    plural: nosqls
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [parameters]
              properties:
                parameters:
                  type: object
                  required: []
                  properties:
                    partitionKey:
                      type: string
                      default: id
                      description: "Partition key attribute name."
                    partitionKeyType:
                      type: string
                      default: S
                      enum: [S, N, B]
                      description: "Partition key type. S=string, N=number, B=binary."
                    dataRetention:
                      type: string
                      default: delete
                      enum: [retain, delete]
                      description: "AWS resource reclaim behavior when the XR is deleted. delete=Crossplane deletes the DynamoDB table and IAM user (data is unrecoverable); retain=Crossplane orphans them (resources persist in AWS, data recoverable)."
