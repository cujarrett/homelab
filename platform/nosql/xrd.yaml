apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: nosqls.platform.local.lab
spec:
  group: platform.local.lab
  scope: Cluster
  names:
    kind: NoSql
    plural: nosqls
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [parameters]
              properties:
                parameters:
                  type: object
                  required: [namespace]
                  properties:
                    partitionKey:
                      type: string
                      default: id
                      description: "Partition key attribute name."
                    partitionKeyType:
                      type: string
                      default: S
                      enum: [S, N, B]
                      description: "Partition key type. S=string, N=number, B=binary."
                    namespace:
                      type: string
                      description: "Namespace of the owning workload. Used for the Namespace cost-allocation tag. The binding Secret itself is written by the referencing Api into its own namespace."
                    dataRetention:
                      type: string
                      default: delete
                      enum: [retain, delete]
                      description: "AWS resource reclaim behavior when the XR is deleted. delete=Crossplane deletes the DynamoDB table and IAM user (data is unrecoverable); retain=Crossplane orphans them (resources persist in AWS, data recoverable)."
