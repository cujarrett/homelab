apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: xsqls.platform.local.lab
spec:
  group: platform.local.lab
  scope: Cluster
  names:
    kind: XSql
    plural: xsqls
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [parameters]
              properties:
                parameters:
                  type: object
                  required: [namespace]
                  properties:
                    size:
                      type: string
                      default: sm
                      enum: [xs, sm, md, lg]
                      description: "Size for the RDS instance (cloud only). xs=db.t4g.micro, sm=db.t4g.small, md=db.t4g.medium, lg=db.t4g.large."
                    backend:
                      type: string
                      default: private-cloud
                      enum: [private-cloud, public-cloud]
                      description: "Backend to provision. public-cloud provisions AWS RDS Postgres; private-cloud provisions in-cluster Postgres."
                    namespace:
                      type: string
                      description: "Namespace to deploy the database into and write binding Secrets to. Must already exist (created by the tenant's namespace.yaml)."
                    dataRetention:
                      type: string
                      default: delete
                      enum: [retain, delete]
                      description: "PVC reclaim behavior when the XR is deleted. retain=longhorn-retain (PVC survives XR deletion, data recoverable); delete=longhorn-delete (PVC wiped on XR deletion, data is unrecoverable). Intentionally destructive when set to delete."
                    consumerServiceAccounts:
                      type: array
                      items:
                        type: string
                      description: "Service account names of the XApis that will bind this database (public-cloud only). Each entry gets its own IAM role and binding secret scoped to that SA's exact SPIFFE ID. Each entry must match an XApi name."
