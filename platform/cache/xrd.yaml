apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: xcaches.platform.local.lab
spec:
  group: platform.local.lab
  scope: Cluster
  names:
    kind: XCache
    plural: xcaches
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                parameters:
                  type: object
                  properties:
                    size:
                      type: string
                      default: sm
                      enum: [xs, sm, md, lg]
                      description: "Size for the cache cluster (cloud only). xs=cache.t4g.micro, sm=cache.t4g.small, md=cache.t4g.medium, lg=cache.t4g.medium."
                    backend:
                      type: string
                      default: cluster
                      enum: [cluster, cloud]
                      description: "Backend to provision. cloud provisions AWS ElastiCache; cluster provisions in-cluster Redis."
                    namespace:
                      type: string
                      description: "Namespace to deploy into. Must already exist (created by the tenant's namespace.yaml)."
