# Credentials for the Hubble UI basicAuth middleware — see
# cluster/monitoring/external-secrets-basic-auth.yaml for why these exist.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: hubble-basic-auth
  namespace: kube-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: hubble-basic-auth
    creationPolicy: Owner
  data:
    - secretKey: users
      remoteRef:
        key: homelab/cluster
        property: hubble_basic_auth_htpasswd
