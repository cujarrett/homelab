apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: secret-mirror-controller
  namespace: argocd
spec:
  project: cluster
  source:
    repoURL: https://github.com/cujarrett/homelab.git
    targetRevision: main
    path: cluster/secret-mirror-controller
  destination:
    server: https://kubernetes.default.svc
    namespace: secret-mirror-controller
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
